```java
// Map<String, List<CloudOrganizationArchivedHospitalUser>> userMap = users.stream().collect(Collectors.groupingBy(CloudOrganizationArchivedHospitalUser::getHospitalId));  
// for (OperateOrganizationArchivedHospitalDTO dto:list){  
//     if (userMap.containsKey(dto.getHospitalId())){  
//         List<CloudOrganizationArchivedHospitalUser>  
//     }  
// }
```

```java
// for (int i = 0; i < list.size(); i++) {  
//     String hospitalId = list.get(i).getHospitalId();  
//  
//     list.get(i).setHospitalUserList(users);  
// }
```

